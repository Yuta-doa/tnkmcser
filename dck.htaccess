RewriteEngine On

# 1. 除外するIPアドレスを指定（あなたのIP）
RewriteCond %{REMOTE_ADDR} !^125\.197\.174\.13

# 2. 除外するCookieを指定（Cookie名が「access_pass」、値が「allowed」の場合）
RewriteCond %{HTTP_COOKIE} !access_pass=allowed

# 3. 上記2つどちらにも該当しない場合、別サイトへ転送（302一時転送）
RewriteRule ^(.*)$ https://example.com [R=302,L]
